#setup camera
set camid 10
camera_create 10 "cam1"

# setup viewport
set vpid  20
viewport_create 20 "viewport1"
viewport_geometry $vpid 0 0 400 300
viewport_attachcamera $vpid $camid 
viewport_add $vpid

# create a rectangle for test
set rcid 100
[rectangle_create 100]
rectangle_size $rcid 2 2
add_child 0 $rcid

update_bbox 0
